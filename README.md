# Advanced-File-Organizer
The Advanced File Organizer is a Python tool with a simple Tkinter interface that helps manage files efficiently. It can organize files into categories, remove duplicates using SHA-256 hashing, and rename files in bulk with a consistent format. The tool displays real-time logs and uses message boxes for task updates.
